copy .vimrc .bashrc .inputrc .zshrc and install oh-my-zsh
---------------------------------
[ 0. yum (or apt-get) install git zsh ]
1. git clone git://github.com/quiwamu/dotfiles ~/dotfiles
2. sh ~/dotfiles/setup.sh